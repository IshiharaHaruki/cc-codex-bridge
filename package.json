{
  "name": "cc-codex-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal localhost bridge: Claude Agent SDK <-> Codex app-server, bidirectional, user-triggered.",
  "scripts": {
    "dev": "tsx src/server.ts",
    "codex:protocol": "codex app-server generate-ts --out src/codex-protocol --experimental"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.177",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
